services:
  bot:
    build: .
    container_name: goldbot

    restart: unless-stopped

    env_file: .env

    volumes:
      - goldbot-data:/data

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  goldbot-data:
